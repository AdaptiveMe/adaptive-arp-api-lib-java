sudo: false
language: java
before_install:
  echo $TRAVIS_BRANCH $TRAVIS_TAG $TRAVIS_PULL_REQUEST
jdk:
  - oraclejdk8
cache:
  directories:
    - /home/travis/.gradle/wrapper/dists/gradle-2.3-all/w01pvxy5gty3tegjrai84pm3
after_success:
  if [ "$TRAVIS_BRANCH" == "master" ] && [ "$TRAVIS_TAG" != "" ] && [ "$TRAVIS_PULL_REQUEST" == "false" ]; then

    echo "This will deploy!"

  else

    echo "This will not deploy!"

  fi